<div class="row">
  <div class="span12">
    <h2>Costumes</h2>
    <p>We know the feeling, you're either desperately looking for the perfect costume, or you're trying to recoup the thousands you've spent on Amazon Prime shipments... either way, we're here to help. Check out your classmates offerings below, and feel free to add some of your own!</p>
               <div class="notice"><%= notice %></div>
                  <%= form_tag '/search' do %>
                  <form class="navbar-search pull-left">
                    <%= text_field_tag :term, "", placeholder: 'Search' %>
                  </form>
                  <% end %>

<p><%= link_to 'Add a New Costume', new_costume_url %></p>

<table class="table table-striped">
  <table style="table-laout: fixed; width: 100%">
  <thread>
    <tr>
      <th align="left">Name</th>
      <th align="left">Description</th>
      <th align="left">Price</th>
      <th align="left">Currency</th>
      <th align="left">Actions</th>
    </tr>
  </thread>
  <tbody>
  <% @costumes.each do |costume| %>
    <tr>
      <td width="10%"><%= link_to costume.name, costume_url(costume) %></td>
      <td style="word-wrap: break-word" width="60%"><%= costume.description %></td>
      <td width="6%"><%= costume.price %></td>
      <td width="10"><%= costume.currency %></td>
      <td width="14">
          <%= link_to 'Edit', edit_costume_url(costume), :class => 'btn btn-mini' %>
          <%= button_to 'Destroy', costume_url(costume), method: 'delete', data: {confirm: "Do you really want to delete this costume?"}, :class => 'btn btn-mini btn-danger' %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>


